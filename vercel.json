{
  "version": 2,
  "installCommand": "echo 'No install needed for Python backend'",
  "buildCommand": "echo 'No build needed - using serverless functions'",
  "builds": [
    {
      "src": "api/index.py",
      "use": "@vercel/python",
      "config": {
        "maxLambdaSize": "15mb"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.py"
    }
  ]
}
