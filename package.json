{
  "name": "igbot2025-backend",
  "version": "1.0.0",
  "description": "Python backend - no npm dependencies",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo 'Python backend uses serverless functions - no build step required' && exit 0"
  },
  "engines": {
    "node": ">=18.x"
  }
}
